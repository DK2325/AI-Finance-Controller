# Shell scripts must stay LF or the Linux containers cannot execute them,
# regardless of what a Windows checkout would otherwise do.
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
Dockerfile* text eol=lf
*.yml text eol=lf
