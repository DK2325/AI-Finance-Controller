venv/
.venv/
__pycache__/
*.py[cod]
.git/
.pytest_cache/
.ruff_cache/
node_modules/
.env
# The image needs the demo batch (one-click run), the training batch (the review screen
# shows narrations and amounts as evidence), and the seeded run. It does NOT need the
# sealed test set, which stays out so the seal keeps meaning something.
data/test/
notes/
tests/
